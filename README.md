# Tự build Linear Regression bằng Numpy(Viết bằng tiếng Việt trước, mai sửa tiếng Anh sau)

Dự án AI đầu tay tự xây dựng thuật toán Linear Regression từ con số 0 bằng toán học (Gradient Descent), không sử dụng thư viện Machine Learning có sẵn.

## Môi trường & Thư viện
* Python 3.x
* Numpy
* Pandas

## Cách chạy thử nghiệm
Cài đặt thư viện và chạy file main:
```bash
pip install numpy pandas
python main.py
